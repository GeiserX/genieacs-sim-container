# CLAUDE.md — genieacs-sim-container

## Overview
Docker containerization for the GenieACS Simulator (genieacs-sim). Provides a ready-to-run container for simulating TR-069 CPE devices against a GenieACS instance, useful for testing and development.

## Tech Stack
- Dockerfile (Node.js 26 Alpine base)
- GenieACS Simulator (upstream: zaidka/genieacs-sim)
- GitHub Actions CI

## Development
```bash
docker build -t genieacs-sim .
docker run genieacs-sim
```

## Architecture
- `Dockerfile` — builds from `node:26-alpine`, clones genieacs-sim from upstream
- `docs/` — documentation and images

## Key Rules
- Docker image published as `drumsergio/genieacs-sim-container` on Docker Hub
- Clones upstream genieacs-sim at build time from master branch
- Part of the broader GenieACS ecosystem (container, MCP, ansible, HA)
- Used for testing TR-069 device simulation against GenieACS

*Generated by [LynxPrompt](https://lynxprompt.com) CLI*
